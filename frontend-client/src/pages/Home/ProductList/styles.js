import { Box, styled } from "@mui/material";

export const Wrapper = styled(Box)({
  paddingTop: 48,
  paddingBottom: 64,
  backgroundColor: '#EBEBEB',
  h2: {
    fontSize: 24,
    fontWeight: 700,
    marginBottom: 8,
    color: '#2D4C2A',
  },
});
