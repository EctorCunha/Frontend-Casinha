import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#2D4C2A',
    },
    secondary: {
      main: '#5B8B1D',
    },
    success: {
      main: '#5B8B1D',
    },
  },
  typography: {
    fontFamily: 'Lato',
  },
});
